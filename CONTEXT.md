# coding-prac 학습 시스템

이 레포에서 작업하는 사람과 AI 튜터가 공유해야 하는 도메인. 학습 시스템은 문서 허브와, AI-guided 학습 세션 (Session) 이 채워 넣을 작은 산출물 슬롯 묶음으로 구성됨.

## 용어

**학습 단위 (Topic)**:
슬러그 (예: `binary-tree`, `big-o`) 로 식별되는 CS 학습의 추상적 단위. 파일이 아니라 주제 그 자체. 같은 슬러그로 묶이는 파일·코드가 이 학습 단위에 매달림.
_피할 표현_: 주제, 항목, 단원.

**학습 자료 (Topic material)**:
학습 단위의 지속 설명 문서. `docs/topics/<slug>/README.md` 에 위치. `docs/TOPIC_FORMAT.md` 가 정의한 7개 필드를 따름.
_피할 표현_: 학습 노트, 설명 문서, 콘텐츠.

**학습 코드 (Implementation artifact)**:
학습 단위의 실행 가능한 학습용 코드. `implementations/<lang>/<slug>/` 에 위치. 학습자가 직접 쓰거나 AI 와 함께 다듬은 코드여야 함. 정답 덤프 아님.
_피할 표현_: 솔루션, 예제 코드.

**풀이 코드 (Solution artifact)**:
학습 단위와 관련된 연습 문제 풀이 코드. `solutions/<slug>/` 에 위치. 학습 코드와 라이프사이클이 분리되어, 기준 구현 코드와 문제 풀이 시도가 같은 파일에 섞이지 않음.
_피할 표현_: 구현, 연습 코드.

**대분류 (Category)**:
학습 단위가 속하는 큰 분류 (예: 자료구조와 알고리즘, 운영체제, 네트워크, 실전). 학습 자료에 명시되고, 진행 상태 문서 (Progress document) 가 뷰에서 그룹핑할 때 노출.
_피할 표현_: 섹션, 그룹, 태그.

**즉흥 학습 단위 (Ad-hoc Topic)**:
계획된 커리큘럼 (Curriculum) 에 없는 학습 단위. 학습자가 즉흥적으로 공부한 CS 지식. 계획된 학습 단위와 동등하게 학습 자료·학습 코드·풀이 코드·진행 상태 기록을 받음.
_피할 표현_: 비계획, 추가 주제.

**세션 (Session)**:
경계가 있는 한 차례의 AI-guided 학습. 학습 지점 (Checkpoint) 을 정하고, 학습 자료나 산출물을 만들거나 다듬은 다음, 세션 요약을 쓰고 진행 상태 문서를 갱신하면서 마감.
_피할 표현_: 시팅, 강의.

**세션 요약 (Session summary)**:
세션의 짧은 지속 기록. `docs/sessions/<YYYY-MM-DD>-<slug>.md` 에 위치. 원본 채팅 로그는 지속 기록이 아니라는 원칙에 따라 이 요약이 대체함.
_피할 표현_: 채팅 로그, 트랜스크립트, 일지.

**커리큘럼 (Curriculum)**:
계획된 1개월 학습 경로. `docs/CURRICULUM.md` 에 위치. 전체 학습 단위 중에서 *계획된* 부분집합을 계획된 순서대로 정의.
_피할 표현_: 로드맵, 실라버스, 계획.

**학습 지점 (Checkpoint)**:
세션이 겨냥하는 다음 학습 목표. 커리큘럼이 정한 다음 단계와 진행 상태 문서가 보여주는 학습자의 현재 위치에서 도출.
_피할 표현_: 마일스톤, 목표.

**진행 상태 문서 (Progress document)**:
`docs/PROGRESS.md`. 활성 학습 단위 전체에 대한 현재 학습 상태의 단일 진실 원천 (source of truth).
_피할 표현_: 대시보드, 상태 파일.

**진행 상태 뷰 (Progress view)**:
`docs/progress.html`. 진행 상태 문서를 로드 시점에 읽어서 렌더링하는 뷰. 자체적으로 학습 상태를 보관하지 않음.
_피할 표현_: 대시보드, 상태 페이지.

**승격 (Promotion)**:
세션에서 만들어진 유용한 콘텐츠를 지속 학습 자료로 옮기는 행위. 규칙은 `docs/TOPIC_FORMAT.md` 에 정의.
_피할 표현_: 이관, 복사.

**증거 링크 (Evidence link)**:
한 지속 문서가 다른 지속 문서를 가리키는 포인터 (보통 진행 상태 문서 → 학습 자료 / 학습 코드 / 풀이 코드 / 세션 요약). 내용을 중복으로 옮겨 적는 대신 링크로 처리.
_피할 표현_: 참조, 인용.

## 관계

- 학습 단위 한 개에 학습 자료 한 개, 학습 코드 0개 이상, 풀이 코드 0개 이상이 매달림.
- 학습 단위는 대분류 하나에 속함.
- 학습 단위는 커리큘럼 멤버이거나, 즉흥 학습 단위 둘 중 하나.
- 세션은 학습 지점 하나를 겨냥하고 세션 요약으로 마감.
- 진행 상태 문서는 활성 학습 단위당 항목 한 개를 갖고, 산출물과 최신 세션 요약으로 가는 증거 링크를 보관.
- 진행 상태 뷰는 진행 상태 문서에서 파생됨. 자체 상태를 만들지 않음.

## 예시 대화

> **학습자**: "오늘 TCP/UDP 공부하고 싶은데, 이게 학습 단위로 등록되나?"
> **AI 튜터**: "커리큘럼엔 없지만 학습 단위 맞아요. 네트워크 대분류의 즉흥 학습 단위. `docs/topics/tcp-udp/` 에 학습 자료 만들고, 코드 쓰면 `implementations/<lang>/tcp-udp/` 에 학습 코드 저장합니다."
>
> **학습자**: "어제 LeetCode 의 이진 트리 문제 풀었는데, 이건 학습 코드에 들어가?"
> **AI 튜터**: "아니요. 그건 풀이 코드라서 `solutions/<problem-slug>/` 에 들어갑니다. 학습 코드는 이진 트리 클래스 자체이고, 풀이 코드는 그 문제의 풀이 시도예요."

## 표시된 모호함

- 영어 "implementation" 은 학습 코드와 문제 풀이 코드 양쪽에 다 쓰일 수 있음. 해소: **학습 코드 (Implementation artifact)** = 학습 단위의 코드만, **풀이 코드 (Solution artifact)** = 문제 풀이 코드. 항상 분리된 표현 사용.
- "학습 단위" 가 주제와 그 문서 둘 다를 가리키며 혼용됐었음. 해소: **학습 단위 (Topic)** = 주제, **학습 자료 (Topic material)** = 문서.
